{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}
<h3>Finalizar Venda</h3>
<div  style= "text-align:right;" >
    <a href="/funcionario/venda"> <img src="/img/voltar.png" width="50px"> </a>
</div>
<hr>
<div class="card mt-4 mx-auto"style="width: 20rem;">
    <div class="card-body">
        <form class="form" action="/funcionario/compra/{{novaVenda._id}}" method="POST">
            <h5>Valor: R${{novaVenda.valor}}</h5>
            <p>Funcionario: {{funcionario.nome}}</p>
            <p>Data: {{novaVenda.horario}}</p>
            {{#if novaVenda.bebida}}
                <p>Bebida: Sim</p>
            {{/if}}
            {{#if novaVenda.suco}}
                <p>Suco: Sim</p> 
            {{/if}}
            {{#if novaVenda.refrigerante}}      
                <p>Refrigerante: Sim</p>
            {{/if}}
            {{#if novaVenda.agua}}
                <p>Água: Sim</p>
            {{/if}}
            <label for="cpf">CPF cliente (opcional):</label>
                <input type="text"  name="cpf" id="cpf" placeholder="xxx.xxx.xxx-xx" class="form-control mt-4 mb-4 w-75">
            <button type="submit" class="btn btn-primary">Finalizar</button>           
        </form>
    </div>   
</div>
